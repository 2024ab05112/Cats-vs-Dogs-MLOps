apiVersion: apps/v1
kind: Deployment
metadata:
  name: cats-vs-dogs-backend
  labels:
    app: cats-vs-dogs-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cats-vs-dogs-backend
  template:
    metadata:
      labels:
        app: cats-vs-dogs-backend
    spec:
      containers:
        - name: backend
          image: YOUR_DOCKERHUB_USERNAME/cats-vs-dogs-api:latest
          ports:
            - containerPort: 8000
          env:
            - name: MLFLOW_TRACKING_URI
              value: "http://mlflow-service:5000"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
